carto_hal.config:

  path: '/admin/config/carto_hal/config'

  defaults:

    _form: '\Drupal\carto_hal\Form\carto_halConfigForm'

    _title: 'Carto Hal Configuration'

  requirements:

    _permission: 'administer site configuration'
